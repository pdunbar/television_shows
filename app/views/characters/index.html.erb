<h3>All Characters</h3>

<ul>
  <% @characters.each do |character| %>
    <li>
      <%= link_to character.character, television_show_path(character.television_show_id) %><%= character.actor %>
    </li>
  <% end %>
</ul>
